on:
  release:
    types: [published]

jobs:
  get-info:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/github-script@v6
      - run: ruby -r ./newfile.rb
