on:
  issues:
    types: [edited]

jobs:
  testing:
    name: issue edited
    runs-on: ubuntu-latest
    steps: 
      - run: |
          echo edit on issue: $ISSUE
        env:
          ISSUE: ${{ github.event.issue.number }}
